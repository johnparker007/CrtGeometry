<Window x:Class="CrtGeometry.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CRT Geometry" Height="700" Width="1180" MinHeight="520" MinWidth="850">
    <TabControl Margin="16">
      <TabItem Header="Games">
       <Grid x:Name="GamesPanel" Margin="8">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="3*"/><RowDefinition Height="2*"/></Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
         <TextBlock FontSize="24" FontWeight="SemiBold" Text="Games Catalogue"/>
         <WrapPanel Margin="0,10,0,10" VerticalAlignment="Center">
          <TextBox Width="300" Margin="0,0,14,0" ToolTip="Search description, ROM name, manufacturer, or year" Text="{Binding SearchText,UpdateSourceTrigger=PropertyChanged}"/>
          <TextBlock Margin="0,4,5,0" Text="Inclusion:"/><ComboBox Width="125" Margin="0,0,14,0" ItemsSource="{Binding InclusionOptions}" SelectedItem="{Binding Inclusion}"/>
          <TextBlock Margin="0,4,5,0" Text="Presence:"/><ComboBox Width="115" Margin="0,0,14,0" ItemsSource="{Binding PresenceOptions}" SelectedItem="{Binding Presence}"/>
          <TextBlock Margin="0,4,5,0" Text="Profile:"/><ComboBox Width="120" Margin="0,0,14,0" ItemsSource="{Binding ProfileOptions}" SelectedItem="{Binding Profile}"/>
          <TextBlock Margin="0,4,0,0" Text="{Binding ResultSummary}"/>
         </WrapPanel>
        </StackPanel>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Games}" SelectedItem="{Binding SelectedGame}" AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" EnableRowVirtualization="True" EnableColumnVirtualization="True" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling">
         <DataGrid.Columns>
          <DataGridTextColumn Header="Description" Binding="{Binding DisplayName}" Width="2*" SortMemberPath="Description"/>
          <DataGridTextColumn Header="ROM" Binding="{Binding RomName}" Width="110"/>
          <DataGridTextColumn Header="Year" Binding="{Binding Year}" Width="70"/>
          <DataGridTextColumn Header="Manufacturer" Binding="{Binding Manufacturer}" Width="*"/>
          <DataGridTextColumn Header="Resolution" Binding="{Binding ResolutionSummary}" Width="145" CanUserSort="False"/>
          <DataGridTextColumn Header="Orientation" Binding="{Binding RotationSummary}" Width="90" CanUserSort="False"/>
          <DataGridTextColumn Header="Refresh" Binding="{Binding RefreshSummary}" Width="105" CanUserSort="False"/>
          <DataGridTextColumn Header="Status" Binding="{Binding InclusionStatus}" Width="150"/>
          <DataGridTextColumn Header="Profile" Binding="{Binding ProfileStatus}" Width="90"/>
         </DataGrid.Columns>
        </DataGrid>
        <GroupBox Grid.Row="2" Header="Selected game details" Margin="0,10,0,0">
         <Grid Margin="8">
          <Grid.ColumnDefinitions><ColumnDefinition Width="280"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
          <StackPanel>
           <TextBlock FontSize="18" FontWeight="SemiBold" Text="{Binding SelectedGame.DisplayName}" TextWrapping="Wrap"/>
           <TextBlock><Run FontWeight="Bold" Text="ROM: "/><Run Text="{Binding SelectedGame.RomName}"/></TextBlock>
           <TextBlock><Run FontWeight="Bold" Text="Year: "/><Run Text="{Binding SelectedGame.Year}"/></TextBlock>
           <TextBlock><Run FontWeight="Bold" Text="Manufacturer: "/><Run Text="{Binding SelectedGame.Manufacturer}"/></TextBlock>
           <TextBlock><Run FontWeight="Bold" Text="Clone of: "/><Run Text="{Binding SelectedGame.CloneOf}"/></TextBlock>
           <TextBlock><Run FontWeight="Bold" Text="Present: "/><Run Text="{Binding SelectedGame.IsPresent}"/></TextBlock>
           <TextBlock Text="{Binding SelectedGame.InclusionStatus}" TextWrapping="Wrap"/>
           <TextBlock><Run FontWeight="Bold" Text="Coin inputs: "/><Run Text="{Binding SelectedGame.CoinInputs,TargetNullValue=Unknown}"/></TextBlock>
           <TextBlock><Run FontWeight="Bold" Text="Assignment: "/><Run Text="{Binding SelectedGame.ProfileStatus}"/></TextBlock>
           <TextBlock><Run FontWeight="Bold" Text="Signature: "/><Run Text="{Binding SelectedGame.VideoMode.Message}"/></TextBlock>
           <TextBlock><Run FontWeight="Bold" Text="Calibration source ROM: "/><Run Text="{Binding SelectedGame.CalibrationSourceRomName,TargetNullValue=None}"/></TextBlock>
           <WrapPanel Margin="0,8,0,0"><TextBox x:Name="ManualProfileId" Width="55" ToolTip="Profile ID (1..255)"/><Button Margin="5,0" Click="ManualAssign_Click">Assign manual</Button><Button Click="ResetOverride_Click">Reset override</Button></WrapPanel>
          </StackPanel>
          <DataGrid Grid.Column="1" Margin="12,0,0,0" ItemsSource="{Binding SelectedGame.Displays}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
           <DataGrid.Columns>
            <DataGridTextColumn Header="Type" Binding="{Binding Type}"/><DataGridTextColumn Header="Width" Binding="{Binding Width}"/><DataGridTextColumn Header="Height" Binding="{Binding Height}"/>
            <DataGridTextColumn Header="Rotate" Binding="{Binding Rotate}"/><DataGridTextColumn Header="Refresh" Binding="{Binding Refresh,StringFormat={}{0:0.######}}"/>
            <DataGridTextColumn Header="Pixel clock" Binding="{Binding PixelClock}"/><DataGridTextColumn Header="HTotal" Binding="{Binding HTotal}"/>
            <DataGridTextColumn Header="HB start" Binding="{Binding HBStart}"/><DataGridTextColumn Header="HB end" Binding="{Binding HBEnd}"/>
            <DataGridTextColumn Header="VTotal" Binding="{Binding VTotal}"/><DataGridTextColumn Header="VB start" Binding="{Binding VBStart}"/><DataGridTextColumn Header="VB end" Binding="{Binding VBEnd}"/>
           </DataGrid.Columns>
          </DataGrid>
         </Grid>
        </GroupBox>
       </Grid>
      </TabItem>
      <TabItem Header="Calibration">
       <Grid x:Name="CalibrationPanel" Margin="8">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="2*"/><RowDefinition Height="Auto"/><RowDefinition Height="2*"/></Grid.RowDefinitions>
        <StackPanel><TextBlock FontSize="24" FontWeight="SemiBold" Text="New calibration"/><TextBlock Text="Search by title, ROM, manufacturer, or year; choose the exact human-readable MAME entry."/><TextBox Margin="0,8" Width="380" HorizontalAlignment="Left" Text="{Binding SearchText,UpdateSourceTrigger=PropertyChanged}"/></StackPanel>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Candidates}" SelectedItem="{Binding SelectedGame}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
         <DataGrid.Columns><DataGridTextColumn Header="Description" Binding="{Binding DisplayName}" Width="2*"/><DataGridTextColumn Header="ROM" Binding="{Binding RomName}" Width="120"/><DataGridTextColumn Header="Year" Binding="{Binding Year}" Width="70"/><DataGridTextColumn Header="Manufacturer" Binding="{Binding Manufacturer}" Width="*"/><DataGridTextColumn Header="Video" Binding="{Binding VideoMode.Message}" Width="250"/></DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="2" Margin="0,8" Orientation="Horizontal"><StackPanel.Resources><Style x:Key="GeometryValueTextBox" TargetType="TextBox"><EventSetter Event="GotKeyboardFocus" Handler="GeometryTextBox_GotKeyboardFocus"/></Style></StackPanel.Resources><TextBlock Margin="0,4,15,0" Text="{Binding VideoModeMessage}"/>
         <TextBlock Margin="4" Text="HSH"/><TextBox Width="38" Style="{StaticResource GeometryValueTextBox}" Text="{Binding HSH}"/><TextBlock Margin="4" Text="VSL"/><TextBox Width="38" Style="{StaticResource GeometryValueTextBox}" Text="{Binding VSL}"/><TextBlock Margin="4" Text="VAM"/><TextBox Width="38" Style="{StaticResource GeometryValueTextBox}" Text="{Binding VAM}"/><TextBlock Margin="4" Text="VSC"/><TextBox Width="38" Style="{StaticResource GeometryValueTextBox}" Text="{Binding VSC}"/><TextBlock Margin="4" Text="VSH"/><TextBox Width="38" Style="{StaticResource GeometryValueTextBox}" Text="{Binding VSH}"/>
         <Button Margin="12,0,4,0" Click="PreviewCalibration_Click">Preview</Button><Button Click="ApplyCalibration_Click">Confirm / apply</Button></StackPanel>
        <DockPanel Grid.Row="3"><StackPanel DockPanel.Dock="Top"><TextBlock FontWeight="Bold" Text="{Binding PreviewSummary}"/><TextBlock Foreground="DarkGreen" Text="{Binding Status}"/></StackPanel><DataGrid ItemsSource="{Binding MatchingGames}" IsReadOnly="True" AutoGenerateColumns="False"><DataGrid.Columns><DataGridTextColumn Header="Description" Binding="{Binding DisplayName}" Width="2*"/><DataGridTextColumn Header="ROM" Binding="{Binding RomName}" Width="*"/><DataGridTextColumn Header="Current assignment" Binding="{Binding ProfileStatus}" Width="150"/></DataGrid.Columns></DataGrid></DockPanel>
       </Grid>
      </TabItem>
      <TabItem Header="Profiles">
       <DockPanel Margin="8">
        <TextBlock DockPanel.Dock="Top" Margin="0,0,0,12" FontSize="24" FontWeight="SemiBold" Text="Geometry Profiles" />
        <StackPanel DockPanel.Dock="Bottom" Margin="0,12,0,0" Orientation="Horizontal">
            <Button Width="90" Margin="0,0,8,0" Click="Add_Click">New</Button><Button Width="90" Margin="0,0,8,0" Click="Save_Click">Save</Button><Button Width="90" Click="Delete_Click">Delete</Button>
            <TextBlock Margin="16,4,0,0" Foreground="DarkGreen" Text="{Binding StatusMessage}" />
        </StackPanel>
        <DataGrid x:Name="ProfilesGrid" ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfile}"
                  AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" Width="60" />
                <DataGridTextColumn Header="HSH" Binding="{Binding HSH, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Width="70" />
                <DataGridTextColumn Header="VSL" Binding="{Binding VSL, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Width="70" />
                <DataGridTextColumn Header="VAM" Binding="{Binding VAM, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Width="70" />
                <DataGridTextColumn Header="VSC" Binding="{Binding VSC, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Width="70" />
                <DataGridTextColumn Header="VSH" Binding="{Binding VSH, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Width="70" />
                <DataGridTextColumn Header="Notes" Binding="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" Width="*" />
                <DataGridTextColumn Header="Calibration source" Binding="{Binding CalibrationSourceTitle}" Width="180" IsReadOnly="True" />
                <DataGridTextColumn Header="Source ROM" Binding="{Binding CalibrationSourceRomName}" Width="100" IsReadOnly="True" />
                <DataGridTextColumn Header="Games" Binding="{Binding AssignedGameCount}" Width="65" IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>
       </DockPanel>
      </TabItem>
      <TabItem Header="MAME Import">
       <StackPanel Margin="16">
        <TextBlock FontSize="24" FontWeight="SemiBold" Text="MAME XML Import" />
        <TextBlock Margin="0,8,0,16" TextWrapping="Wrap" Text="Import an older &lt;game&gt; or newer &lt;machine&gt; MAME -listxml catalogue. Existing imported metadata is updated by ROM shortname." />
        <Button x:Name="ImportButton" Width="160" Height="32" HorizontalAlignment="Left" Click="Import_Click">Import MAME XML...</Button>
        <ProgressBar x:Name="ImportProgress" Margin="0,16,0,4" Height="16" IsIndeterminate="False" />
        <TextBlock x:Name="ImportStatus" Text="No import has been run in this session." />
        <TextBox x:Name="ImportSummary" Margin="0,16,0,0" MinHeight="180" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" />
       </StackPanel>
      </TabItem>
      <TabItem Header="Data">
       <StackPanel Margin="16">
        <TextBlock FontSize="24" FontWeight="SemiBold" Text="CSV Import / Export" />
        <TextBlock Margin="0,8,0,16" TextWrapping="Wrap" Text="Export or restore application-owned profiles, calibration history, active signature mappings, and assignments. SQLite remains authoritative; the MAME catalogue is not included." />
        <WrapPanel>
         <Button Width="150" Height="32" Margin="0,0,8,0" Click="ExportCsv_Click">Export CSV ZIP...</Button>
         <Button Width="150" Height="32" Margin="0,0,16,0" Click="ValidateCsv_Click">Import CSV ZIP...</Button>
         <TextBlock Margin="0,7,6,0" Text="Mode:"/><ComboBox x:Name="CsvMode" Width="210" SelectedIndex="0"><ComboBoxItem Content="Merge"/><ComboBoxItem Content="Replace user configuration"/></ComboBox>
        </WrapPanel>
        <TextBlock x:Name="CsvStatus" Margin="0,16,0,6" Text="No CSV set selected." FontWeight="SemiBold"/>
        <TextBox x:Name="CsvSummary" MinHeight="220" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
        <Button x:Name="ApplyCsvButton" Width="150" Height="32" Margin="0,12,0,0" HorizontalAlignment="Left" IsEnabled="False" Click="ApplyCsv_Click">Apply import</Button>
       </StackPanel>
      </TabItem>
      <TabItem Header="Firmware / Arduino">
       <StackPanel Margin="16">
        <TextBlock FontSize="24" FontWeight="SemiBold" Text="Arduino Game Database" />
        <TextBlock Margin="0,8,0,16" TextWrapping="Wrap" Text="Preview and generate profiles plus assigned, included, present MAME games as a compact AVR PROGMEM database. This does not compile or upload firmware." />
        <TextBlock FontWeight="Bold" Text="Firmware directory"/>
        <DockPanel Margin="0,5,0,12">
         <Button DockPanel.Dock="Right" Width="100" Margin="8,0,0,0" Click="BrowseFirmware_Click">Browse...</Button>
         <TextBox x:Name="FirmwareDirectory" IsReadOnly="True" VerticalContentAlignment="Center"/>
        </DockPanel>
        <TextBlock><Run FontWeight="Bold" Text="Output: "/><Run x:Name="FirmwareOutputPath"/></TextBlock>
        <Grid Margin="0,16,0,12" MaxWidth="500" HorizontalAlignment="Left">
         <Grid.ColumnDefinitions><ColumnDefinition Width="260"/><ColumnDefinition Width="100"/></Grid.ColumnDefinitions>
         <Grid.RowDefinitions><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/></Grid.RowDefinitions>
         <TextBlock Text="Profile count"/><TextBlock Grid.Column="1" x:Name="FirmwareProfileCount"/>
         <TextBlock Grid.Row="1" Text="Highest profile ID"/><TextBlock Grid.Row="1" Grid.Column="1" x:Name="FirmwareHighestId"/>
         <TextBlock Grid.Row="2" Text="Profile table estimate"/><TextBlock Grid.Row="2" Grid.Column="1" x:Name="FirmwareTableBytes"/>
         <TextBlock Grid.Row="3" Text="Validity bitmap estimate"/><TextBlock Grid.Row="3" Grid.Column="1" x:Name="FirmwareValidityBytes"/>
         <TextBlock Grid.Row="4" Text="Generated games"/><TextBlock Grid.Row="4" Grid.Column="1" x:Name="FirmwareGameCount"/>
         <TextBlock Grid.Row="5" Text="Packed names"/><TextBlock Grid.Row="5" Grid.Column="1" x:Name="FirmwareNameBytes"/>
         <TextBlock Grid.Row="6" Text="32-bit offsets"/><TextBlock Grid.Row="6" Grid.Column="1" x:Name="FirmwareOffsetBytes"/>
         <TextBlock Grid.Row="7" Text="Game/profile mapping"/><TextBlock Grid.Row="7" Grid.Column="1" x:Name="FirmwareMappingBytes"/>
         <TextBlock Grid.Row="8" Text="Alphabet jump table"/><TextBlock Grid.Row="8" Grid.Column="1" x:Name="FirmwareJumpBytes"/>
         <TextBlock Grid.Row="9" Text="Average name length"/><TextBlock Grid.Row="9" Grid.Column="1" x:Name="FirmwareAverageName"/>
         <TextBlock Grid.Row="10" Text="Longest name"/><TextBlock Grid.Row="10" Grid.Column="1" x:Name="FirmwareLongestName"/>
         <TextBlock Grid.Row="11" FontWeight="Bold" Text="Total generated data"/><TextBlock Grid.Row="11" Grid.Column="1" FontWeight="Bold" x:Name="FirmwareTotalBytes"/>
        </Grid>
        <WrapPanel><Button Width="170" Height="32" Click="GenerateFirmware_Click">Generate Database</Button><Button Width="120" Height="32" Margin="8,0,0,0" Click="RefreshFirmware_Click">Refresh stats</Button></WrapPanel>
        <TextBlock x:Name="FirmwareStatus" Margin="0,14,0,0" TextWrapping="Wrap" FontWeight="SemiBold"/>
       </StackPanel>
      </TabItem>
    </TabControl>
</Window>
